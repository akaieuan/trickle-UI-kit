{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "grain",
  "title": "Grain",
  "description": "Radial dot mask dissolve — the word forms out of a field of dots.",
  "files": [
    {
      "path": "registry/default/grain/grain.tsx",
      "content": "import { type ElementType, type CSSProperties } from 'react';\nimport { cn } from '@/lib/cn';\n\nexport interface GrainProps {\n  children: React.ReactNode;\n  /** Starting dot size in pixels — smaller = finer grain. Default 5. */\n  dotSize?: number;\n  /** Animation delay in ms. */\n  delay?: number;\n  /** Total dissolve duration in ms. */\n  duration?: number;\n  /** Element tag. */\n  as?: ElementType;\n  className?: string;\n}\n\nexport function Grain({\n  children,\n  dotSize = 5,\n  delay = 0,\n  duration = 1600,\n  as: Component = 'span',\n  className\n}: GrainProps) {\n  return (\n    <Component\n      className={cn('inline-block animate-trickle-grain-dissolve', className)}\n      style={{\n        animationDelay: `${delay}ms`,\n        animationDuration: `${duration}ms`,\n        ['--trickle-grain-dot-size' as string]: `${dotSize}px`\n      } as CSSProperties}\n    >\n      {children}\n    </Component>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/trickle/grain.tsx"
    }
  ],
  "cssVars": {
    "theme": {
      "--animate-trickle-grain-dissolve": "trickle-grain-dissolve 1.6s cubic-bezier(0.4, 0, 0.2, 1) both"
    }
  },
  "css": {
    ".animate-trickle-grain-dissolve": {
      "-webkit-mask-image": "radial-gradient(circle at center, black 35%, transparent 75%)",
      "mask-image": "radial-gradient(circle at center, black 35%, transparent 75%)",
      "-webkit-mask-repeat": "repeat",
      "mask-repeat": "repeat",
      "-webkit-mask-position": "center center",
      "mask-position": "center center"
    },
    "@keyframes trickle-grain-dissolve": {
      "0%": {
        "-webkit-mask-size": "var(--trickle-grain-dot-size, 5px) var(--trickle-grain-dot-size, 5px)",
        "mask-size": "var(--trickle-grain-dot-size, 5px) var(--trickle-grain-dot-size, 5px)",
        "opacity": "0",
        "filter": "blur(0.4px)"
      },
      "20%": {
        "opacity": "1",
        "filter": "blur(0.4px)"
      },
      "60%": {
        "-webkit-mask-size": "60% 60%",
        "mask-size": "60% 60%",
        "opacity": "1",
        "filter": "blur(0.2px)"
      },
      "100%": {
        "-webkit-mask-size": "800% 800%",
        "mask-size": "800% 800%",
        "opacity": "1",
        "filter": "blur(0)"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      ".animate-trickle-grain-dissolve": {
        "animation": "none",
        "-webkit-mask-image": "none",
        "mask-image": "none"
      }
    }
  },
  "type": "registry:ui"
}