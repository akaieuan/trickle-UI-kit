{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "bounce",
  "title": "Bounce",
  "description": "Letters bounce in from above with overshoot physics. Per-char stagger.",
  "files": [
    {
      "path": "registry/default/bounce/bounce.tsx",
      "content": "import { type ElementType, type CSSProperties } from 'react';\nimport { cn } from '@/lib/cn';\n\nexport interface BounceProps {\n  text: string;\n  delay?: number;\n  stagger?: number;\n  as?: ElementType;\n  className?: string;\n}\n\nexport function Bounce({ text, delay = 0, stagger = 60, as: Component = 'span', className }: BounceProps) {\n  return (\n    <Component className={cn('inline-block', className)} aria-label={text}>\n      {text.split('').map((char, i) => (\n        <span\n          key={i}\n          aria-hidden='true'\n          className='inline-block whitespace-pre animate-trickle-bounce'\n          style={{ animationDelay: `${delay + i * stagger}ms` } as CSSProperties}\n        >\n          {char}\n        </span>\n      ))}\n    </Component>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/trickle/bounce.tsx"
    }
  ],
  "cssVars": {
    "theme": {
      "--animate-trickle-bounce": "trickle-bounce 0.9s cubic-bezier(0.34, 1.56, 0.64, 1) both"
    }
  },
  "css": {
    "@keyframes trickle-bounce": {
      "0%": {
        "opacity": "0",
        "transform": "translateY(-1.2em) scale(0.6)"
      },
      "50%": {
        "opacity": "1",
        "transform": "translateY(0.15em) scale(1.05)"
      },
      "70%": {
        "transform": "translateY(-0.05em) scale(0.98)"
      },
      "85%": {
        "transform": "translateY(0.02em) scale(1.01)"
      },
      "100%": {
        "opacity": "1",
        "transform": "translateY(0) scale(1)"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      ".animate-trickle-bounce": {
        "animation": "none",
        "opacity": "1",
        "transform": "none"
      }
    }
  },
  "type": "registry:ui"
}