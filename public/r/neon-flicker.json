{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "neon-flicker",
  "title": "Neon Flicker",
  "description": "Neon glow with irregular flicker. Pure CSS, infinite loop.",
  "files": [
    {
      "path": "registry/default/neon-flicker/neon-flicker.tsx",
      "content": "import { type ElementType, type CSSProperties } from 'react';\nimport { cn } from '@/lib/cn';\n\nexport interface NeonFlickerProps {\n  children: React.ReactNode;\n  /** Neon glow color (any CSS color). Default warm pink. */\n  color?: string;\n  /** Full flicker cycle duration in ms. Default 3000. */\n  duration?: number;\n  /** Glow intensity multiplier on the halo radii. 1 = stock tube. Default 1. */\n  glow?: number;\n  /** Element tag. */\n  as?: ElementType;\n  className?: string;\n}\n\nexport function NeonFlicker({\n  children,\n  color = 'oklch(72% 0.22 350)',\n  duration = 3000,\n  glow = 1,\n  as: Component = 'span',\n  className\n}: NeonFlickerProps) {\n  // Three stacked halos: tight core, mid bloom, wide ambient spill.\n  const halo = [4, 12, 32].map((r) => `0 0 ${r * glow}px ${color}`).join(', ');\n\n  return (\n    <Component\n      className={cn('inline-block animate-trickle-neon-flicker', className)}\n      style={{\n        color,\n        animationDuration: `${duration}ms`,\n        textShadow: halo\n      } as CSSProperties}\n    >\n      {children}\n    </Component>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/trickle/neon-flicker.tsx"
    }
  ],
  "cssVars": {
    "theme": {
      "--animate-trickle-neon-flicker": "trickle-neon-flicker 3s linear infinite"
    }
  },
  "css": {
    "@keyframes trickle-neon-flicker": {
      "0%, 1%": {
        "opacity": "0.05",
        "filter": "brightness(0.2) blur(0.3px)",
        "text-shadow": "none"
      },
      "2%": {
        "opacity": "0.9",
        "filter": "brightness(1.1)"
      },
      "3%, 4%": {
        "opacity": "0.05",
        "filter": "brightness(0.2) blur(0.3px)",
        "text-shadow": "none"
      },
      "5%": {
        "opacity": "0.7",
        "filter": "brightness(0.85)"
      },
      "6%, 7%": {
        "opacity": "0.1",
        "filter": "brightness(0.3) blur(0.3px)"
      },
      "8%": {
        "opacity": "0.95",
        "filter": "brightness(1.05)"
      },
      "9%": {
        "opacity": "0.4",
        "filter": "brightness(0.5)"
      },
      "11%": {
        "opacity": "1",
        "filter": "brightness(1.15)"
      },
      "13%, 14%": {
        "opacity": "0.5",
        "filter": "brightness(0.65)"
      },
      "16%": {
        "opacity": "1",
        "filter": "brightness(1.1)"
      },
      "20%, 55%": {
        "opacity": "1",
        "filter": "brightness(1)"
      },
      "56%, 57%": {
        "opacity": "0.85",
        "filter": "brightness(0.9)"
      },
      "58%, 80%": {
        "opacity": "1",
        "filter": "brightness(1)"
      },
      "81%": {
        "opacity": "0.6",
        "filter": "brightness(0.75)"
      },
      "82%, 100%": {
        "opacity": "1",
        "filter": "brightness(1)"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      ".animate-trickle-neon-flicker": {
        "animation": "none",
        "opacity": "1"
      }
    }
  },
  "type": "registry:ui"
}