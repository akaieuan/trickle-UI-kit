{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "wave",
  "title": "Wave",
  "description": "Letters undulate vertically in a sine-wave pattern. Pure CSS, infinite loop.",
  "files": [
    {
      "path": "registry/default/wave/wave.tsx",
      "content": "import { type ElementType, type CSSProperties } from 'react';\nimport { cn } from '@/lib/cn';\n\nexport interface WaveProps {\n  text: string;\n  /** Cycle duration in ms. */\n  duration?: number;\n  /** Delay between adjacent characters' phases, in ms. */\n  stagger?: number;\n  /** Element tag. */\n  as?: ElementType;\n  className?: string;\n}\n\nexport function Wave({\n  text,\n  duration = 1800,\n  stagger = 80,\n  as: Component = 'span',\n  className\n}: WaveProps) {\n  return (\n    <Component className={cn('inline-block', className)} aria-label={text}>\n      {text.split('').map((char, i) => (\n        <span\n          key={i}\n          aria-hidden='true'\n          className='inline-block whitespace-pre animate-trickle-wave'\n          style={{\n            animationDelay: `${i * stagger}ms`,\n            animationDuration: `${duration}ms`\n          } as CSSProperties}\n        >\n          {char}\n        </span>\n      ))}\n    </Component>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/trickle/wave.tsx"
    }
  ],
  "cssVars": {
    "theme": {
      "--animate-trickle-wave": "trickle-wave 1.8s ease-in-out infinite"
    }
  },
  "css": {
    "@keyframes trickle-wave": {
      "0%, 100%": {
        "transform": "translateY(0)"
      },
      "50%": {
        "transform": "translateY(-0.3em)"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      ".animate-trickle-wave": {
        "animation": "none",
        "transform": "none"
      }
    }
  },
  "type": "registry:ui"
}