{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "typo-correct",
  "title": "Typo Correct",
  "description": "A word flies in chaotically, lands misspelled, then corrects itself character-by-character.",
  "registryDependencies": [
    "@trickle/text-root"
  ],
  "files": [
    {
      "path": "registry/default/typo-correct/typo-correct.tsx",
      "content": "'use client';\n\nimport { useEffect, useState, type ElementType } from 'react';\nimport { cn } from '@/lib/cn';\nimport { useTextRoot } from '@/registry/default/text-root/text-root';\n\ntype Phase = 'hidden' | 'flying' | 'misspelled' | 'correcting' | 'done';\n\nexport interface TypoCorrectProps {\n  /** The correct word that the animation resolves to. */\n  word: string;\n  /** A misspelling of `word`. Must be the same length and contain the same letters. */\n  misspelled: string;\n  /** Delay in ms before the chaotic fly-in begins. */\n  delay?: number;\n  /** Duration of the chaotic fly-in animation, in ms. */\n  flyDuration?: number;\n  /** Pause in ms after letters land in misspelled order, before correction starts. */\n  pauseAfterFly?: number;\n  /** Delay in ms between each correction step. */\n  charDelay?: number;\n  /** Element tag to render. Defaults to `span`. */\n  as?: ElementType;\n  className?: string;\n  /** Trigger source: `mount` (default) or `view`. */\n  trigger?: 'mount' | 'view';\n}\n\nexport function TypoCorrect({\n  word,\n  misspelled,\n  delay = 300,\n  flyDuration = 1000,\n  pauseAfterFly = 500,\n  charDelay = 120,\n  as: Component = 'span',\n  className,\n  trigger = 'mount'\n}: TypoCorrectProps) {\n  if (process.env.NODE_ENV !== 'production' && word.length !== misspelled.length) {\n    console.warn(\n      `[trickle/TypoCorrect] \"word\" (${word.length}) and \"misspelled\" (${misspelled.length}) must be the same length.`\n    );\n  }\n\n  const { ready, prefersReducedMotion, setRef } = useTextRoot({ trigger, delay });\n  const [phase, setPhase] = useState<Phase>('hidden');\n  const [correctedChars, setCorrectedChars] = useState(0);\n\n  useEffect(() => {\n    if (!ready || prefersReducedMotion) return;\n\n    setPhase('flying');\n    const timers: number[] = [];\n\n    timers.push(window.setTimeout(() => setPhase('misspelled'), flyDuration));\n\n    const correctionStart = flyDuration + pauseAfterFly;\n    timers.push(window.setTimeout(() => setPhase('correcting'), correctionStart));\n\n    for (let i = 1; i <= word.length; i++) {\n      timers.push(\n        window.setTimeout(() => {\n          setCorrectedChars(i);\n          if (i === word.length) setPhase('done');\n        }, correctionStart + i * charDelay)\n      );\n    }\n\n    return () => timers.forEach(window.clearTimeout);\n  }, [ready, prefersReducedMotion, flyDuration, pauseAfterFly, charDelay, word.length]);\n\n  if (prefersReducedMotion || phase === 'done') {\n    return (\n      <Component ref={setRef} className={className}>\n        {word}\n      </Component>\n    );\n  }\n\n  if (!ready || phase === 'hidden') {\n    return (\n      <Component ref={setRef} className={cn(className, 'opacity-0')}>\n        {word}\n      </Component>\n    );\n  }\n\n  const flyVariant = (i: number) => `animate-trickle-letter-fly-${(i % 4) + 1}`;\n\n  if (phase === 'flying') {\n    return (\n      <Component ref={setRef} className={cn(className, 'inline-block')}>\n        {misspelled.split('').map((char, i) => (\n          <span key={`fly-${i}`} className={cn('inline-block', flyVariant(i))}>\n            {char}\n          </span>\n        ))}\n      </Component>\n    );\n  }\n\n  if (phase === 'misspelled') {\n    return (\n      <Component ref={setRef} className={cn(className, 'inline-block')}>\n        {misspelled.split('').map((char, i) => (\n          <span key={`mis-${i}`} className='inline-block animate-trickle-letter-land'>\n            {char}\n          </span>\n        ))}\n      </Component>\n    );\n  }\n\n  return (\n    <Component ref={setRef} className={cn(className, 'inline-block')}>\n      {word.split('').map((correctChar, i) => {\n        const isRevealed = i < correctedChars;\n        const shown = isRevealed ? correctChar : (misspelled[i] ?? correctChar);\n        return (\n          <span\n            key={`correct-${i}`}\n            className={cn('inline-block', isRevealed && 'animate-trickle-precise')}\n          >\n            {shown}\n          </span>\n        );\n      })}\n    </Component>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/trickle/typo-correct.tsx"
    }
  ],
  "cssVars": {
    "theme": {
      "--animate-trickle-letter-fly-1": "trickle-letter-fly-1 1s cubic-bezier(0.34, 1.56, 0.64, 1) forwards",
      "--animate-trickle-letter-fly-2": "trickle-letter-fly-2 1s cubic-bezier(0.34, 1.56, 0.64, 1) forwards",
      "--animate-trickle-letter-fly-3": "trickle-letter-fly-3 1s cubic-bezier(0.34, 1.56, 0.64, 1) forwards",
      "--animate-trickle-letter-fly-4": "trickle-letter-fly-4 1s cubic-bezier(0.34, 1.56, 0.64, 1) forwards",
      "--animate-trickle-letter-land": "trickle-letter-land 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) forwards",
      "--animate-trickle-precise": "trickle-precise 0.4s cubic-bezier(0.16, 1, 0.3, 1) both"
    }
  },
  "css": {
    "@keyframes trickle-letter-fly-1": {
      "0%": {
        "transform": "translate(-200px, -120px) rotate(-180deg)",
        "opacity": "0"
      },
      "60%": {
        "opacity": "1"
      },
      "100%": {
        "transform": "translate(0, 0) rotate(0)",
        "opacity": "1"
      }
    },
    "@keyframes trickle-letter-fly-2": {
      "0%": {
        "transform": "translate(180px, 90px) rotate(220deg)",
        "opacity": "0"
      },
      "60%": {
        "opacity": "1"
      },
      "100%": {
        "transform": "translate(0, 0) rotate(0)",
        "opacity": "1"
      }
    },
    "@keyframes trickle-letter-fly-3": {
      "0%": {
        "transform": "translate(-150px, 100px) rotate(150deg)",
        "opacity": "0"
      },
      "60%": {
        "opacity": "1"
      },
      "100%": {
        "transform": "translate(0, 0) rotate(0)",
        "opacity": "1"
      }
    },
    "@keyframes trickle-letter-fly-4": {
      "0%": {
        "transform": "translate(220px, -80px) rotate(-200deg)",
        "opacity": "0"
      },
      "60%": {
        "opacity": "1"
      },
      "100%": {
        "transform": "translate(0, 0) rotate(0)",
        "opacity": "1"
      }
    },
    "@keyframes trickle-letter-land": {
      "0%": {
        "transform": "translateY(-4px)"
      },
      "60%": {
        "transform": "translateY(2px)"
      },
      "100%": {
        "transform": "translateY(0)"
      }
    },
    "@keyframes trickle-precise": {
      "0%": {
        "opacity": "0",
        "transform": "scale(0.5)"
      },
      "40%": {
        "opacity": "0.3",
        "transform": "scale(0.8)"
      },
      "70%": {
        "opacity": "0.8",
        "transform": "scale(1.05)"
      },
      "100%": {
        "opacity": "1",
        "transform": "scale(1)"
      }
    },
    "@media (prefers-reduced-motion: reduce)": {
      "[class*=\"animate-trickle-letter-fly\"], .animate-trickle-letter-land, .animate-trickle-precise": {
        "animation": "none",
        "opacity": "1",
        "transform": "none"
      }
    }
  },
  "type": "registry:ui"
}